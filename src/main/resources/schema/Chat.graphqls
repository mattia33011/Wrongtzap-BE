
extend type Query{
    Chat (chatId: String!): Chat!
    everyChat: [Chat!]
}

scalar LocalDateTime

type Chat{

    chatId: String!
    name: String!
    participants : [UserResponse]!
    participantsDate: [JoinDate!]!
    messages: [MessageResponse!]
    isGroup: Boolean!
}

type JoinDate{
    userId: String!
    timestamp: LocalDateTime!
}

type ChatResponse{
    name: String!
    chatId: String!
}