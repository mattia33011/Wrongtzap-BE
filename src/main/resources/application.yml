spring:
  application:
    name: wrongtzap
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/users?createDatabaseIfNotExist=true&useSSL=true
    username: root
    password: root